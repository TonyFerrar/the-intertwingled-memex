---
title: "Future Backlog"
publish: false
---

# Future Backlog

- [ ] Configure the GitHub Pages site to use a custom subdomain (e.g., `memex.tonyferrar.com` or `notes.tonyferrar.com`).
  - [ ] Add a CNAME record at the DNS provider pointing to `tonyferrar.github.io`.
  - [ ] Create a `CNAME` file in `web-portal/public/` containing the custom subdomain.
  - [ ] Update `site` and `base` in `web-portal/astro.config.mjs` to reflect the new domain.
  - [ ] Configure the custom domain in the GitHub Pages settings repository page.
